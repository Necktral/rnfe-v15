[pytest]
testpaths = tests
markers =
    requires_torch: test requiere dependencia torch.
    requires_postgres: test requiere PostgreSQL disponible y configurado.
    requires_cuda: test requiere GPU CUDA funcional.
