# Archive Policy

`archive/` es cuarentena técnica:

- No tiene poder gobernante.
- No se importa desde `runtime/` ni desde `exocortex/`.
- Solo se usa para trazabilidad histórica y referencia.
- Cualquier rescate desde `archive/` debe pasar por `PROMOTE` o `REWRITE`.

